# Changelog — 2026-04-04

## Summary
Major infrastructure day: unified storage, per-project chat, NAS sync engine, auth fixes, SMTP security.

---

## Commits (in order)

### 1. `31c29bf` — refactor: consolidate storage paths to unified STORAGE_ROOT
**Files created:** `controllers/storageConfig.js`
**Files changed:** `controllers/fileMeta.js`, `controllers/folderSync.js`, `controllers/projectCreator.js`, `routes/dataRoutes.js`, `routes/fileRoutes.js`, `routes/moduleRoutes.js`, `routes/projectRoutes.js`, `server.js`, `.gitignore`
**What:** Removed all `BASE_DRIVE`/`F:/Supreme` references. Created single `storageConfig.js` as source of truth. All project data now under `STORAGE_ROOT` (default: `./storage/`). Added one-time migration helper for legacy files.

### 2. `3f1c40d` — refactor: per-project chat database
**Files changed:** `controllers/chatDb.js` (full rewrite), `routes/chatRoutes.js`, `server.js`
**What:** Each project gets its own `chat.db` in `storage/<project>/chat/`. Lazy connection pool (max 20 DBs, LRU eviction). One-time migration from old single DB. Graceful shutdown hooks.

### 3. `a4b870e` — feat: NAS background sync engine
**Files created:** `controllers/nasSync.js`, `controllers/nasOnDemand.js`
**Files changed:** `routes/dataRoutes.js`, `routes/fileRoutes.js`, `routes/moduleRoutes.js`, `routes/adminRoutes.js`, `server.js`, `package.json`
**What:** WebDAV-based background sync to NAS. 48h cleanup of synced files on VPS. On-demand fetch from NAS for cleaned files. Admin endpoints for sync status/trigger. Disabled by default.

### 4. `a814e0c` — feat: admin NAS sync monitor
**Files changed:** `admin.html`
**What:** Sync status card with live indicator, stats grid, Sync Now button, collapsible error log. Auto-refresh every 30s.

### 5. `c0b6ffc` — fix: QA bug fixes
**Files changed:** `controllers/nasSync.js`, `routes/moduleRoutes.js`, `routes/chatRoutes.js`, `admin.html`
**What:** 
- CRITICAL: `.db` files excluded from NAS sync cleanup (would have deleted chat DBs)
- Path traversal fix in moduleRoutes list-files/clear-files
- Multer async callback fix in chatRoutes
- Admin error display fix (was showing [object Object])

### 6. `3163a54` — fix: auth redirect on all pages
**Files changed:** `src/js/dashboard.js`, `dashboard.html`, `files.html`, `admin.html`
**What:** Added auth redirects to 4 unprotected pages. Admin page also checks for admin role. All 13 protected pages now verified.

### 7. `f490409` — feat: add dotenv for .env auto-loading
**Files changed:** `server.js`, `package.json`
**What:** Added `dotenv` dependency. `.env` file auto-loaded on server start.

### 8. `28db22c` — security: remove hardcoded SMTP defaults
**Files changed:** `routes/adminRoutes.js`, `routes/authRoutes.js`
**What:** Removed hardcoded Kasserver defaults from SMTP config. All credentials now require `.env` file.
